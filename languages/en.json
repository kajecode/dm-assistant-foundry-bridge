{
  "DM-ASSISTANT-BRIDGE": {
    "settings": {
      "baseUrl": {
        "name": "dm-assistant base URL",
        "hint": "HTTP endpoint of your dm-assistant instance. Use http://localhost:5000 when running on the same host."
      },
      "apiKey": {
        "name": "dm-assistant API key",
        "hint": "Optional. Required only if your dm-assistant is exposed beyond localhost and gated behind an API key."
      },
      "campaignId": {
        "name": "Campaign ID",
        "hint": "The dm-assistant campaign to pull from. Picked from a dropdown sourced from your server when reachable; otherwise falls back to free text."
      },
      "useCampaignCodex": {
        "name": "Use Campaign Codex when available",
        "hint": "When checked and Campaign Codex is installed, route imports through CC's sheet types. Uncheck to always use native Foundry documents."
      },
      "folderPrefix": {
        "name": "Import folder prefix",
        "hint": "Prefix for the auto-created Foundry folders that group imported content by kind. Example: 'DM Assistant' produces 'DM Assistant — NPCs', 'DM Assistant — Creatures', 'DM Assistant — Shops', etc. Empty falls back to 'DM Assistant'."
      },
      "dataPathPrefix": {
        "name": "Foundry Data/ upload prefix",
        "hint": "Subfolder under Foundry's Data/ directory where imported NPC portraits and thumbnails are uploaded. Example: 'dm-assistant' puts files at Data/dm-assistant/<campaign>/npc/<slug>.png."
      },
      "itemCompendiums": {
        "name": "Item compendiums to resolve against",
        "hint": "When set, imported weapons / spells / equipment whose name matches a compendium item are replaced with the fully-statted compendium document (e.g. real Fireball mechanics from the dnd5e SRD or a DDB Importer pack). Empty = off (items stay LLM stubs). 'auto' = search every Item compendium. Or a comma-separated list of pack ids, e.g. 'dnd5e.items, world.homebrew'. An explicit compendium_source from dm-assistant always wins."
      }
    },
    "keybindings": {
      "openImportPicker": {
        "name": "Open dm-assistant import picker",
        "hint": "Opens the NPC picker dialog (DM-only)."
      }
    }
  }
}
